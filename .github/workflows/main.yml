name: Step 1 - Hello World
   on: [push]
   jobs:
     build:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - name: Say hello
           run: echo "Hello, GitHub Actions!"